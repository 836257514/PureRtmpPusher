<Window x:Class="Direct3DViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Image x:Name="imgHoster" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <CheckBox IsChecked="{Binding EnableHardwareDecode}" Content="开启硬编码" VerticalAlignment="Center" Margin="5"/>
            <Button Content="{Binding Name}" DataContext="{Binding PlayCommand}" Command="{Binding}" Height="30" Width="60" Margin="5"/>
            <Button Content="{Binding Name}" DataContext="{Binding StopCommand}" Command="{Binding}" Height="30" Width="60" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
